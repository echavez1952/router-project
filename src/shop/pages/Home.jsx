import React from "react";

export const Home = () => {


  return (
    <div>

      <h1>Estoy en el home</h1>
      {/* banner */}
      {/* Filtros */}

      {/* <div style={{ display: "flex", gap: "1rem", padding: "2rem" }}>
        {productos.map((p, i) => (
          <ProductCard key={i} {...p} />
        ))}
      </div> */}
    </div>
  );
};
